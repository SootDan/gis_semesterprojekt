<!DOCTYPE html>
<html lang="<%= locale %>">
    <%- include("templates/head") %>
    <body>
        <div id="accountInfo" data-info="<%= JSON.stringify(database) %>"></div>
        <%- include("templates/navbar") %>
        <!--TODO: Add JS to detect time of day and have dynamic greeting -->
        <h3><%=__("greeting_morning") %>, <%= database.name %>!</h2>
            <table>
                <thead>
                    <th>
                        <%=__("subject") %>
                    </th>
                    <th>
                        <%=__("hours") %>
                    </th>
                    <th>
                        <%=__("time_spent_studying") %>
                    </th>
                    <th>
                        <%=__("hours") %> / <%=__("day") %>
                    </th>
                    <th>
                        <%=__("hours") %> / <%=__("week") %>
                    </th>
                    <th>
                        <%=__("hours") %> / <%=__("month") %>
                    </th>
                    <th>
                        Deadline
                    </th>
                    <th>
                        Misc
                    </th>
                </thead>
                
                <tbody id="subj_table">
                </tbody>
            </table>
        <%- include("templates/footer") %>
        <script type="module" src="<%= scriptPath %>" defer></script>
    </body>
</html>